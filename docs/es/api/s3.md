# Referencia de la API Compatible con S3

## Descripción General

Memesplora proporciona una API compatible con AWS S3 para su uso con cualquier cliente compatible con S3.

## Endpoint

`http://localhost:5213`

## Operaciones Soportadas

### Bucket
- `GET /` - ListBuckets
- `HEAD /{bucket}` - HeadBucket

### Objetos
- `GET /{bucket}/{key}` - GetObject
- `PUT /{bucket}/{key}` - PutObject
- `DELETE /{bucket}/{key}` - DeleteObject
- `HEAD /{bucket}/{key}` - HeadObject
- `GET /{bucket}` - ListObjects / ListObjectsV2
- `POST /{bucket}?delete` - DeleteObjects

### Cargas Multiparte
- `POST /{bucket}/{key}?uploads` - CreateMultipartUpload
- `PUT /{bucket}/{key}?partNumber=&uploadId=` - UploadPart
- `POST /{bucket}/{key}?uploadId=` - CompleteMultipartUpload
- `DELETE /{bucket}/{key}?uploadId=` - AbortMultipartUpload
- `GET /{bucket}/{key}?uploadId=` - ListParts

## Ejemplos

### AWS CLI
```bash
aws configure --profile memesplora
aws s3 --endpoint-url http://localhost:5213 ls
```

### rclone
```bash
rclone config  # Seleccione S3 Compatible
rclone copy file.txt memesplora:my-space/
```